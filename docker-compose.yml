services:
  parsing-service:
    build: .
    ports:
      - "8010:8000"
    env_file:
      - .env
    volumes:
      # debug-артефакты видны на хосте (только при DEBUG=true)
      - ./app/debug:/app/app/debug
    restart: unless-stopped
